<nav id="nav-route" class="navbar fixed-top navbar-light bg-light">
  <a class="navbar-brand" href="/route-search">
    <img src="/images/purple-stars.png" width="30" height="30" class="d-inline-block align-top" alt="logo"
      loading="lazy">
    {{title}}
  </a>
  <form method="POST" action="/logout">
    <button id="logout-route" class="btn btn-info" type="submit">Log Out</button>
  </form>
</nav>
<div style="margin-top: 10vh">
  <div class="fluid-container">

    <h2>You have the power to edit</h2>
    <br>

    <div class="row no-gutters">
      <div class="col-md" <form action="/streetarts/{{streetart._id}}/edit" method="POST" enctype="multipart/form-data">

        <div class="menu-image h-100 d-flex align-items-start pl-5">

          <img src={{imageUrl}} class="img-fluid" alt={{title}}>
        </div>
      </div>
      <div style="margin-right: 10vw;">
        <div class="col-md">
          <div class="menu-text">
            <div class="main-title text-left">
              <label class="menu-content d-flex space-between">
                Artist
                <input type="text" name="artist" value="{{streetart.artist}}">
              </label>

              <label class="menu-content d-flex space-between">
                Name or Description
                <textarea name="description" cols="30" rows="2">{{streetart.name_description}}</textarea>
              </label>

              <label class="menu-content d-flex space-between">
                Address
                <input type="double" name="address" value="{{streetart.address}}">
              </label>

              <label class="menu-content d-flex space-between">
                Latitude
                <input type="string" name="latitude" value="{{streetart.latitude}}">
              </label>

              <label class="menu-content d-flex space-between">
                Longitude
                <input type="string" name="longitude" value="{{streetart.longitude}}">
              </label>

              <label class="menu-content d-flex space-between">
                Other Location Notes
                <textarea name="comment" cols="30" rows="2">{{streetart.other_location_note}}</textarea>
              </label>

              <label class="menu-content d-flex space-between">
                Comment
                <textarea name="comment" cols="30" rows="2">{{streetart.comment}}</textarea>
              </label>
              <button type="submit" class="update-button">Update</button>
            </div>
          </div>
        </div>
        </form>
      </div>
    </div>
    <br>
  </div>
</div>
<div class="links-create">
  <a href="/route-search"> ⇦ Back to route map</a>
  <br>
  <a href="/streetarts"> ⇦ List of all street art</a>
</div>
</div>